# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual environment
venv/
env/
.venv/
*.env  # For virtualenv specific .env files, not the .env for application config

# Application Environment File (contains secrets or local settings)
.env

# IDEs and editors
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS-generated files
.DS_Store
Thumbs.db

# Build artifacts and distribution
dist/
build/
*.egg-info/
*.tar.gz
*.zip
*.whl

# Test artifacts
htmlcov/
.coverage
.pytest_cache/
nosetests.xml
coverage.xml
*.cover

# Uploaded files
# The application saves uploaded files to 'uploaded_documents'
# This directory should be ignored if created locally during development.
# If it's only managed via Docker volumes in production, this still helps keep the repo clean.
uploaded_documents/

# Log files
*.log
logs/

# Other
.Python
celerybeat-schedule